# WaveSpeedAI 数字人API调用指南

本文档基于 WaveSpeedAI 官方文档整理，介绍如何使用API创建和控制数字人（Digital Human/Virtual Avatar）。

## 概述

WaveSpeedAI 提供完整的数字人生成解决方案，支持从零创建具有自然表情和唇同步能力的虚拟主播/数字人。

**核心流程（三阶段）**:
1. **图像生成** - 创建数字人形象
2. **语音生成** - 生成配音音频
3. **动画合成** - 将音频与图像结合，生成唇同步视频

**技术优势**:
- 超快生成速度（视频生成 < 2分钟）
- 自然的面部表情和唇同步
- 支持多角色对话场景
- 企业级API稳定性

---

## API基础配置

### 认证方式

所有API请求需要在HTTP Header中添加：

```http
Authorization: Bearer <YOUR_API_KEY>
Content-Type: application/json
```

### 统一端点

```
POST https://api.wavespeed.ai/api/v3/{model_uuid}
```

### 获取API密钥

访问: https://wavespeed.ai/accesskey

---

## 阶段1: 图像生成（创建数字人形象）

### 推荐模型

**bytedance/seedream-v4** (文本到图像模型)

### API调用示例

```python
import requests

API_KEY = "your_api_key_here"
API_URL = "https://api.wavespeed.ai/api/v3/bytedance/seedream-v4"

headers = {
    "Authorization": f"Bearer {API_KEY}",
    "Content-Type": "application/json"
}

payload = {
    "prompt": "年轻女性数字人（22-28岁），自然妆容，白色衬衫和浅灰色西装外套，专业形象，高清肖像",
    "negative_prompt": "低质量，模糊，变形",
    "width": 1024,
    "height": 1024,
    "num_inference_steps": 50,
    "guidance_scale": 7.5
}

response = requests.post(API_URL, headers=headers, json=payload)
result = response.json()

# 获取生成的图像URL
image_url = result["output"]["image_url"]
```

### 其他可用模型

- **Hunyuan Avatar**
- **Bytedance Avatar Omni Human** (v1.0 & v1.5)
- **Longcat Avatar**
- **Instant Character**

---

## 阶段2: 语音生成（TTS配音）

### 推荐模型

**minimax/speech-02-hd** (文本到语音高清模型)

### API调用示例

```python
TTS_URL = "https://api.wavespeed.ai/api/v3/minimax/speech-02-hd"

tts_payload = {
    "text": "大家好，我是你们的AI主播，今天为大家带来最新的科技资讯。",
    "voice_id": "Wise_Woman",  # 声音角色
    "speed": 1.0,              # 语速（0.5-2.0）
    "pitch": 0,                # 音调（-12 到 12）
    "emotion": "neutral",      # 情绪：happy/sad/angry/neutral
    "english_normalization": True,  # 英文数字自然发音
    "channel": 1,              # 1=单声道，2=立体声
    "bitrate": 128000,         # 音频比特率
    "sample_rate": 24000       # 采样率
}

tts_response = requests.post(TTS_URL, headers=headers, json=tts_payload)
tts_result = tts_response.json()

# 获取生成的音频URL
audio_url = tts_result["output"]["audio_url"]
```

### 可用声音角色示例

- `Wise_Woman` - 智慧女性
- `Young_Male` - 年轻男性
- `Professional_Female` - 职业女性
- （更多角色请查阅官方文档）

### 其他TTS模型

- **Elevenlabs** (Eleven V3, Flash V2/V2.5, Turbo V2/V2.5)
- **Kwaivgi Kling TTS**

---

## 阶段3: 动画合成（唇同步）

### 推荐模型

**wavespeed-ai/infinitetalk** - 专为数字人配音设计的高质量唇同步模型

### API调用示例

```python
LIPSYNC_URL = "https://api.wavespeed.ai/api/v3/wavespeed-ai/infinitetalk"

lipsync_payload = {
    "image_url": image_url,    # 阶段1生成的图像
    "audio_url": audio_url,    # 阶段2生成的音频
    "mask_image": None,        # 可选：定义可动画区域的蒙版
    "prompt": "",              # 可选：额外定制指令
    "seed": 42                 # 随机种子（确保可重复性）
}

lipsync_response = requests.post(LIPSYNC_URL, headers=headers, json=lipsync_payload)
task_id = lipsync_response.json()["task_id"]

# 轮询任务状态
import time

STATUS_URL = f"https://api.wavespeed.ai/api/v3/tasks/{task_id}"

while True:
    status_response = requests.get(STATUS_URL, headers=headers)
    status = status_response.json()

    if status["state"] == "completed":
        video_url = status["output"]["video_url"]
        print(f"✅ 数字人视频生成完成: {video_url}")
        break
    elif status["state"] == "failed":
        print(f"❌ 生成失败: {status['error']}")
        break

    print("⏳ 生成中...")
    time.sleep(5)  # 每5秒检查一次
```

### 核心参数说明

| 参数 | 类型 | 说明 |
|------|------|------|
| `image_url` | string | 数字人形象图片URL |
| `audio_url` | string | 配音音频URL |
| `mask_image` | string/null | 可动画区域蒙版（可选） |
| `prompt` | string | 额外定制指令（可选） |
| `seed` | integer | 随机种子，用于结果可重复性 |

### 其他唇同步模型

**单角色唇同步**:
- **Bytedance Lipsync** (音频转视频)
- **Kwaivgi Kling Lipsync** (音频+文本转视频)
- **Sync Lipsync** (v1.9.0 Beta, v2, v2 Pro)
- **Veed Lipsync**
- **Pixverse Lipsync**
- **Latentsync**

**多角色对话**:
- **infinitetalk/multi** - 支持双角色场景
- **Multitalk** - 多角色对话

---

## 多角色对话场景

### infinitetalk/multi 模型

支持两个数字人的对话场景，需要提供两个角色的图像。

### API调用示例

```python
MULTI_URL = "https://api.wavespeed.ai/api/v3/wavespeed-ai/infinitetalk/multi"

multi_payload = {
    "image_url_left": "https://example.com/avatar1.png",   # 左侧角色
    "image_url_right": "https://example.com/avatar2.png",  # 右侧角色
    "audio_url": audio_url,
    "speaking_mode": "left_right"  # 说话顺序
}

# speaking_mode 可选值:
# - "left_right": 左侧先说，右侧后说
# - "right_left": 右侧先说，左侧后说
# - "meanwhile": 同时说话
```

---

## 完整工作流示例

```python
import requests
import time

class DigitalHumanGenerator:
    def __init__(self, api_key):
        self.api_key = api_key
        self.headers = {
            "Authorization": f"Bearer {api_key}",
            "Content-Type": "application/json"
        }

    def generate_avatar(self, prompt):
        """生成数字人形象"""
        url = "https://api.wavespeed.ai/api/v3/bytedance/seedream-v4"
        payload = {
            "prompt": prompt,
            "width": 1024,
            "height": 1024
        }
        response = requests.post(url, headers=self.headers, json=payload)
        return response.json()["output"]["image_url"]

    def generate_voice(self, text, voice_id="Wise_Woman"):
        """生成配音"""
        url = "https://api.wavespeed.ai/api/v3/minimax/speech-02-hd"
        payload = {
            "text": text,
            "voice_id": voice_id,
            "speed": 1.0,
            "emotion": "neutral"
        }
        response = requests.post(url, headers=self.headers, json=payload)
        return response.json()["output"]["audio_url"]

    def animate_avatar(self, image_url, audio_url):
        """生成唇同步视频"""
        url = "https://api.wavespeed.ai/api/v3/wavespeed-ai/infinitetalk"
        payload = {
            "image_url": image_url,
            "audio_url": audio_url,
            "seed": 42
        }
        response = requests.post(url, headers=self.headers, json=payload)
        task_id = response.json()["task_id"]

        # 轮询等待完成
        status_url = f"https://api.wavespeed.ai/api/v3/tasks/{task_id}"
        while True:
            status = requests.get(status_url, headers=self.headers).json()
            if status["state"] == "completed":
                return status["output"]["video_url"]
            elif status["state"] == "failed":
                raise Exception(status["error"])
            time.sleep(5)

    def create_digital_human(self, avatar_prompt, speech_text, voice_id="Wise_Woman"):
        """完整流程：从提示词到数字人视频"""
        print("🎨 生成数字人形象...")
        image_url = self.generate_avatar(avatar_prompt)

        print("🎙️ 生成配音...")
        audio_url = self.generate_voice(speech_text, voice_id)

        print("🎬 生成唇同步视频...")
        video_url = self.animate_avatar(image_url, audio_url)

        print(f"✅ 完成！视频地址: {video_url}")
        return video_url

# 使用示例
generator = DigitalHumanGenerator("your_api_key")
video = generator.create_digital_human(
    avatar_prompt="专业女性主播，25岁，职业装",
    speech_text="大家好，欢迎收看今天的新闻播报。",
    voice_id="Wise_Woman"
)
```

---

## 成本估算

根据WaveSpeedAI的定价（具体价格请查阅最新官方文档）：

| 服务 | 估算成本 |
|------|---------|
| 图像生成 (Seedream v4) | ~$0.02-0.05/张 |
| 语音生成 (MiniMax TTS) | ~$0.01-0.03/分钟 |
| 唇同步视频 (Infinitetalk) | ~$0.10-0.20/分钟 |

**单个数字人视频（1分钟）总成本**: 约 $0.13-0.28

---

## 最佳实践

### 1. 图像质量优化
- 使用清晰的提示词描述面部特征
- 建议分辨率: 1024x1024
- 避免复杂背景，专注于人物特写

### 2. 语音自然度
- 选择与数字人性别/年龄匹配的voice_id
- 适当调整语速（0.9-1.1之间最自然）
- 使用标点符号控制停顿节奏

### 3. 唇同步效果
- 确保图像中面部清晰可见
- 音频质量直接影响同步精度
- 可使用mask_image精确控制动画区域

### 4. 性能优化
- 使用固定seed值确保结果可重复
- 异步处理多个任务提高效率
- 合理设置轮询间隔（推荐5秒）

---

## 错误处理

### 常见错误码

| 错误码 | 原因 | 解决方案 |
|--------|------|----------|
| 401 | API密钥无效 | 检查Authorization header |
| 429 | 请求频率过高 | 添加重试机制和间隔 |
| 500 | 服务器错误 | 稍后重试 |

### 重试策略示例

```python
import time
from requests.exceptions import RequestException

def api_call_with_retry(url, headers, payload, max_retries=3):
    for attempt in range(max_retries):
        try:
            response = requests.post(url, headers=headers, json=payload)
            response.raise_for_status()
            return response.json()
        except RequestException as e:
            if attempt == max_retries - 1:
                raise
            wait_time = (attempt + 1) * 5
            print(f"请求失败，{wait_time}秒后重试...")
            time.sleep(wait_time)
```

---

## 参考资源

### 官方文档
- **API文档**: https://wavespeed.ai/docs/docs-api
- **快速入门**: https://wavespeed.ai/docs/docs-quick-start
- **数字人教程**: https://wavespeed.ai/blog/posts/create-an-ai-anchor-5-minutes/
- **模型列表**: https://wavespeed.ai/docs/docs

### 技术支持
- **邮箱**: support@wavespeed.ai
- **GitHub**: https://github.com/wavespeedai

### 相关博客
- [WaveSpeed AI Explained: Multimodal Acceleration Platform & APIs](https://skywork.ai/blog/wavespeed-ai-multimodal-acceleration-api/)
- [WaveSpeed AI Review 2025: Ultra-Fast Image & Video Generation Tested](https://skywork.ai/blog/wavespeed-ai-review-2025/)

---

## 总结

WaveSpeedAI 的数字人API提供了完整的端到端解决方案：

1. **简单易用** - 三步即可生成数字人视频
2. **高性能** - 视频生成 < 2分钟
3. **高质量** - 自然的表情和精准的唇同步
4. **灵活扩展** - 支持多角色、多场景

适用于：
- 新闻播报/虚拟主播
- 教育培训视频
- 营销推广内容
- 客服机器人
- 多语言内容本地化

---

**文档更新日期**: 2025-12-30
**基于版本**: WaveSpeedAI API v3
